import SwiftUI

enum AppTheme {
    /// Dark brand color — #2e2226
    static let dark = Color(red: 0x2e / 255.0, green: 0x22 / 255.0, blue: 0x26 / 255.0)

    /// Light brand color — #f5f4ed
    static let light = Color(red: 0xf5 / 255.0, green: 0xf4 / 255.0, blue: 0xed / 255.0)

    /// Accent green — #2d5016
    static let accent = Color(red: 0x2d / 255.0, green: 0x50 / 255.0, blue: 0x16 / 255.0)
}
