Merge branch `claude/monastery-donation-payments-r5onx` into main and push to deploy.

This branch adds `rahulio/pages/donate.html` — a donation demo page for Gaden Shartse Gungru Khangtsen Monastery.

The previous instruction (merge-donate-page.md) failed because it used `master` instead of `main`. This is the corrected version.

Steps:
1. `git fetch origin claude/monastery-donation-payments-r5onx`
2. `git checkout main`
3. `git pull origin main`
4. `git merge origin/claude/monastery-donation-payments-r5onx --no-edit`
5. `git push origin main`

The page will be live at: https://alpacaplayhouse.com/rahulio/pages/donate.html
